# 品質ダッシュボード実装計画

## 1. 目的

現行のFQマップとフレームマップを基盤として、次の調査フローを一続きで実行できるダッシュボードへ拡張する。

1. 把握：品質の分布と推移を確認
2. 判定：調査すべきロットを選定
3. 診断：何が、どこで、いつから、どのように異なるかを特定
4. 提案：類似事例と過去の原因・対応を提示

## 2. 実装方針

- 現行のFQマップと5ロット比較フレームマップは概要画面として維持する。
- 規格不適合と通常状態からの逸脱を別の指標として扱う。
- 通常状態の計算では対象ロットを除外し、未来のロットを参照しない。
- 測定単位のNG率と個片単位の良品率を明確に分離する。
- 絶対値だけでなく、通常中央値、通常との差、Robust Z-scoreを同じ選択状態で比較できるようにする。
- 画面間の選択状態は、ロット、FrameNo、vision、カテゴリ、検査項目を共通コンテキストとして同期する。
- 分析処理、データアクセス、UI構築を分離し、既存コードを必要以上に複雑化しない。

## 3. 優先順位

| 優先度 | マイルストーン | 到達状態                                                   |
| ------ | -------------- | ---------------------------------------------------------- |
| P0     | M1 判定基盤    | 調査対象ロットと、その判定理由を一覧できる                 |
| P0     | M2 基本診断    | 対象ロットを通常状態と比較し、主要因と発生位置を確認できる |
| P1     | M3 詳細診断    | 分布、相関、周期性、個片順序から差の形を説明できる         |
| P1     | M4 類似事例    | 類似異常ロットと過去の原因・対応を確認できる               |
| P2     | M5 運用機能    | 調査状態の保存、共有、出力、性能監視ができる               |

---

## 4. Phase 0：指標定義とデータモデル

### 4.1 良否・適合性の定義

- [ ] 測定適合率を「規格対象となる測定行のうち規格内だった割合」として定義する。
- [ ] 個片良品率を「同一 `lot_number × FrameNo × PositionX × PositionY` の全spec測定が規格内だった個片の割合」として定義する。
- [ ] vision別、カテゴリ別、検査項目別のNG率を個片良品率とは別に保持する。
- [ ] フレーム良品率とロット良品率の集計規則を決定する。
- [ ] 判定画面の規格適合率には個片良品率を使用する。
- [ ] 現行の「総合NG率」が測定行単位であることを画面上に明記する。
- [ ] 規格マージンの符号と尺度を定義し、`spec_position`、`spec_usage`から一貫して算出できるようにする。
- [ ] 両側規格、上限規格、下限規格、規格なしの各ケースについて期待値をテストで固定する。

### 4.2 通常状態と異常度の定義

- [ ] 通常状態の参照窓 `N` の初期値と変更可能範囲を決定する。
- [ ] 対象ロットより前の比較可能なNロットだけを通常状態として選択する。
- [ ] 通常中心を中央値として計算する。
- [ ] 通常ばらつきを `1.4826 × MAD` として計算する。
- [ ] Robust Z-scoreを `(対象値 - 通常中心) / 通常ばらつき` として計算する。
- [ ] MADが0となる場合の扱いを定義し、テストで固定する。
- [ ] 初期ロットなど参照ロット数が不足する場合の表示規則を決定する。
- [ ] 正方向と負方向の双方を扱う指標では絶対異常度と方向付き異常度を保持する。
- [ ] ロット総合異常度の集約方法を決定する。
- [ ] 調査優先度の算出に、規格不適合、異常度、影響個片数、影響項目数を使用する。
- [ ] 閾値をコード内に分散させず、1か所の設定として管理する。

### 4.3 比較対象を揃えるためのメタデータ

- [ ] `meta_unit` を追加し、生値の単位を表示できるようにする。
- [ ] aux項目を含むテストデータを追加し、規格判定を行わず分析対象には含められることを確認する。
- [ ] raw生成、標準化、マニフェスト、テストを追加列に合わせて更新する。

### 4.5 Phase 0の受け入れ条件

- [ ] 同一ロットについて測定適合率と個片良品率が別々に算出される。
- [ ] 対象ロットの異常度計算に対象ロット自身と未来ロットが含まれない。
- [ ] 単位、比較条件、個片順序、事例情報を後続画面から参照できる。
- [ ] raw・standardizedデータのスキーマテストが通る。

---

## 5. Phase 1：ロット判定の分析基盤

### 5.1 集計処理

- [ ] `src/analysis/lot_judgement.py` を追加する。
- [ ] ロット別の測定適合率、個片良品率、NG個片数を集計する。
- [ ] vision、カテゴリ、検査項目別の適合性を集計する。
- [ ] ロット、FrameNo、検査項目ごとの通常中央値とMADを計算する。
- [ ] 平均、標準偏差、NG率、規格マージンに対するRobust Z-scoreを計算する。
- [ ] 空間マップに対する異常セル率と最大連結領域サイズを計算する。
- [ ] ロットごとの総合異常度と調査優先度を計算する。
- [ ] 「規格不適合のみ」「通常逸脱のみ」「両方」「通常」の4分類を付与する。
- [ ] 各ロットについて異常寄与度上位のvision、カテゴリ、検査項目、指標を保持する。
- [ ] 集計結果を `data/analysis` に保存できるようにする。
- [ ] 元データ更新時だけ再計算できる簡潔なキャッシュ方式を追加する。

### 5.2 QualityRepositoryの拡張

- [ ] ロット判定一覧を取得するメソッドを追加する。
- [ ] 指定ロットの通常参照ロット一覧を取得するメソッドを追加する。
- [ ] 指定ロットの異常寄与度一覧を取得するメソッドを追加する。
- [ ] 個片良否を取得するメソッドを追加する。
- [ ] 選択FrameNoのPositionX/Yマップを取得するメソッドを追加する。
- [ ] 生値と規格値を分布表示用に取得するメソッドを追加する。
- [ ] aux項目を含む分析クエリとspec限定の判定クエリを分離する。

### 5.3 分析テスト

- [ ] 個片内に複数NGがあってもNG個片を1件として数えるテストを追加する。
- [ ] 規格値ちょうどを規格内とする境界テストを追加する。
- [ ] 通常参照窓から対象ロットと未来ロットが除外されるテストを追加する。
- [ ] 中央値、MAD、Robust Z-scoreの既知値テストを追加する。
- [ ] 4分類と調査優先度のテストを追加する。
- [ ] 合成データの既知異常ロットが上位候補に現れることを確認する。
- [ ] 異常ラベルを分析入力に使用していないことを確認する。

### 5.4 Phase 1の受け入れ条件

- [ ] 全ロットに規格適合率、異常度、分類、優先度、主要因が付与される。
- [ ] 1件のロットを選択すると、そのロットだけの判定理由を再構成できる。
- [ ] 同じ入力データと設定から同じ判定結果が得られる。

---

## 6. Phase 2：ロット判定画面

### 6.1 画面構成

- [ ] `dashboard.py` に「概要」「判定」「診断」「類似事例」の画面切替を追加する。
- [ ] 現行のFQマップとフレームマップを「概要」画面として維持する。
- [ ] 「判定」画面を追加する。
- [ ] 横軸を個片規格適合率、縦軸を総合異常度とするロット散布図を追加する。
- [ ] 規格不適合のみ、通常逸脱のみ、両方、通常を色で区別する。
- [ ] 調査優先度を点サイズまたは強調度で表現する。
- [ ] 閾値線と4領域の意味を凡例で表示する。
- [ ] ロット番号、日時、適合率、異常度、分類、優先度、主要因をツールチップに表示する。
- [ ] 散布図と同じ内容を優先度順のロット一覧でも表示する。
- [ ] 日付範囲、vision、カテゴリ、検査項目、判定分類で絞り込めるようにする。
- [ ] 通常参照窓Nと異常閾値を確認できるようにする。
- [ ] ロット選択を散布図、一覧、概要画面、診断画面で同期する。

### 6.2 判定理由の表示

- [ ] 選択ロットの規格不適合要因上位を表示する。
- [ ] 選択ロットの通常逸脱要因上位を表示する。
- [ ] vision、カテゴリ、検査項目、指標、異常方向、寄与度を表示する。
- [ ] 影響個片数、影響フレーム数、異常セル率を表示する。
- [ ] 「なぜ目立っているか」を1～3行の定型文で要約する。
- [ ] 通常参照に使用したロット数と期間を表示する。
- [ ] 通常参照不足や比較条件不一致を警告表示する。

### 6.3 UIテスト

- [ ] 4分類が凡例と同じ色で表示されることを確認する。
- [ ] 散布図の点選択で一覧と共通コンテキストが更新されることを確認する。
- [ ] 一覧選択で概要画面と診断画面の対象ロットが更新されることを確認する。
- [ ] 絞り込み後も適合率と異常度の定義が変わらないことを確認する。
- [ ] 通常参照不足のロットが通常ロットとして誤表示されないことを確認する。

### 6.4 Phase 2の受け入れ条件

- [ ] 調査担当者が優先ロットと判定理由を1画面で確認できる。
- [ ] 規格不適合と通常逸脱を混同せずに識別できる。
- [ ] 選択ロットを1操作で診断画面へ引き継げる。

---

## 7. Phase 3：基本診断画面

### 7.1 共通コンテキスト

- [ ] ロット、FrameNo、vision、カテゴリ、検査項目、品質指標の選択状態を管理する。
- [ ] 画面を移動しても選択状態を維持する。
- [ ] 選択状態を画面上部に常時表示する。
- [ ] 集計単位がロット、24フレーム集約、単一フレーム、個片のどれかを明示する。

### 7.2 what：異常項目の特定

- [ ] 異常寄与度ランキングを表示する。
- [ ] vision、カテゴリ、検査項目でランキングを展開できるようにする。
- [ ] NG率差、規格マージン差、平均差、ばらつき差を並べて表示する。
- [ ] ランキング選択をすべての診断グラフへ反映する。
- [ ] spec項目とaux項目を識別できるようにする。

### 7.3 how：通常との差の確認

- [ ] 表示モードとして「対象」「通常中央値」「差分」「Robust Z-score」を追加する。
- [ ] 対象ロットと通常状態を共通スケールで並べる比較ビューを追加する。
- [ ] 生値ヒストグラムを追加する。
- [ ] 規格線と通常中央値をヒストグラムに重ねる。
- [ ] ECDFまたは箱ひげ図を追加し、外れ値と分布変化を確認できるようにする。
- [ ] 対象と通常の件数、平均、中央値、標準偏差、MADを表示する。
- [ ] 水準変化、ばらつき変化、NG率増加のどれが主要かを表示する。

### 7.4 where：位置の特定

- [ ] 現行の24フレーム集約マップに「24フレーム集約」と明記する。
- [ ] 選択FrameNoだけを表示するXYマップを追加する。
- [ ] 対象XYマップ、通常中央値マップ、差分マップ、Z-scoreマップを切り替えられるようにする。
- [ ] XYセルのホバーにPositionX、PositionY、値、通常値、Z-score、件数を表示する。
- [ ] XYセルをクリックして個片詳細を開けるようにする。
- [ ] 空間異常領域の輪郭またはマーカーを表示する。
- [ ] 選択ロット名とFrameNoをXYマップの近くに表示する。

### 7.5 when：発生開始位置の特定

- [ ] ロット順序の推移グラフを追加する。
- [ ] フレーム順序の推移グラフを追加する。
- [ ] 個片順序の推移グラフを追加する。
- [ ] 通常中央値と通常範囲を帯で表示する。
- [ ] 規格線を表示する。
- [ ] 最初に異常閾値を超えたロット、FrameNo、個片順序を示す。
- [ ] 一時的な逸脱と継続的な逸脱を区別する。

### 7.6 個片詳細

- [ ] 選択個片の全vision・全検査項目を一覧表示する。
- [ ] 生値、単位、規格下限、規格上限、規格マージン、良否を表示する。
- [ ] 同一座標の前後フレームまたは前後個片と比較できるようにする。
- [ ] NG項目と通常逸脱項目を別の印で強調する。

### 7.7 Phase 3の受け入れ条件

- [ ] 選択ロットについて主要な異常項目を特定できる。
- [ ] 通常状態との差を絶対値、差分、Z-scoreで確認できる。
- [ ] 異常が現れたFrameNo、PositionX、PositionYを特定できる。
- [ ] 24フレーム集約マップと単一フレームマップを誤認しない表示になっている。

---

## 8. Phase 4：詳細診断

### 8.1 q間関係

- [ ] 個片キーで検査項目を横持ち化する分析処理を追加する。
- [ ] 対象ロットと通常状態の相関行列を計算する。
- [ ] 相関差分行列を表示する。
- [ ] 相関の出現、消失、符号反転を上位候補として表示する。
- [ ] 選択した2項目の散布図を対象と通常で比較できるようにする。

### 8.2 周期性

- [ ] FrameNo方向と個片順序方向の自己相関を計算する。
- [ ] 必要に応じて周波数スペクトルを計算する。
- [ ] 通常状態との差が大きい周期を上位候補として表示する。
- [ ] データ数が不足する場合は周期性を評価不能として表示する。

### 8.3 変化点

- [ ] 水準変化とばらつき変化の変化点候補を計算する。
- [ ] 変化点候補をロット、FrameNo、個片順序のグラフへ表示する。
- [ ] 単一セルの逸脱と連続区間の逸脱を区別する。
- [ ] 変化点の前後で分布を比較できるようにする。

### 8.4 Phase 4の受け入れ条件

- [ ] 相関変化、周期性変化、水準変化、ばらつき変化を区別して確認できる。
- [ ] 詳細分析の結果から元のロット、フレーム、個片へ戻れる。
- [ ] 評価不能な分析を異常なしとして扱わない。

---

## 9. Phase 5：類似事例と対応提案

### 9.1 異常シグネチャ

- [ ] what、how、where、whenを表す特徴量を定義する。
- [ ] vision・カテゴリ・検査項目別の異常寄与を特徴量化する。
- [ ] 水準、ばらつき、NG率、相関、周期性の変化を特徴量化する。
- [ ] XY位置、FrameNo、個片順序の異常範囲を特徴量化する。
- [ ] 異常の大きさと形状を分離して比較できるようにする。
- [ ] 欠損している特徴量を類似度0として扱わず、計算対象外として正規化する。

### 9.2 類似度

- [ ] 総合類似度を計算する。
- [ ] what、how、where、when別の類似度内訳を計算する。
- [ ] 同一ロット自身を検索結果から除外する。
- [ ] 正常ロットと異常事例ロットを区別して検索できるようにする。
- [ ] 類似度の重みを設定として管理する。
- [ ] 類似理由を人が読める短文で表示する。

### 9.3 類似事例画面

- [ ] 類似異常ロットTop Kを表示する。
- [ ] 総合類似度と内訳を表示する。
- [ ] 対象ロット、類似異常ロット、正常参照ロットの3者比較を追加する。
- [ ] 3者でF方向、XY方向、P方向、分布を同じスケールで比較する。
- [ ] 過去原因、実施対応、対応結果を表示する。
- [ ] 類似事例を診断画面の比較対象として選択できるようにする。

### 9.4 事例登録

- [ ] 調査状態を更新できるようにする。
- [ ] 原因と対応を登録できるようにする。
- [ ] 対応結果を登録できるようにする。
- [ ] 登録内容の更新日時を保持する。
- [ ] 未確定情報と確定情報を区別する。

### 9.5 Phase 5の受け入れ条件

- [ ] 対象異常に対して類似ロットと類似理由が表示される。
- [ ] 類似度の内訳から、何が似ているかを確認できる。
- [ ] 過去の原因・対応・結果を対象ロットの調査に利用できる。

---

## 10. Phase 6：運用・品質

### 10.1 操作性

- [ ] 日付、ロット、vision、カテゴリ、検査項目の共通フィルタを追加する。
- [ ] 選択中のフィルタと基準ロットを常時確認できるようにする。
- [ ] フィルタを一括解除できるようにする。
- [ ] 色だけに依存せず、記号とラベルでも判定分類を識別できるようにする。
- [ ] 色範囲が対象内自動調整か全体固定かを明示する。
- [ ] 読み込み中、対象データなし、分析不能の状態を明示する。

### 10.2 出力と共有

- [ ] 選択ロットの診断サマリーを画像またはPDFで出力できるようにする。
- [ ] 判定一覧と異常寄与度をCSVで出力できるようにする。
- [ ] 出力物に対象データ期間、通常参照条件、閾値を記録する。
- [ ] 調査状態、原因、対応を含む簡易レポートを生成できるようにする。

### 10.3 性能

- [ ] 現行画面の起動時間と主要操作時間を計測する。
- [ ] 判定画面、診断画面、類似事例画面の性能目標を決定する。
- [ ] 全31,104,000行をUI操作のたびに再集計しない構成にする。
- [ ] ロット判定と通常統計を事前集計またはキャッシュする。
- [ ] 詳細データは選択ロットと参照ロットだけ遅延取得する。
- [ ] キャッシュ更新条件と破棄条件をテストする。

### 10.4 テストとレビュー

- [ ] 分析関数の単体テストを追加する。
- [ ] Repositoryクエリの集計結果テストを追加する。
- [ ] 画面間の選択同期テストを追加する。
- [ ] 代表画面サイズでスクリーンショットを取得する。
- [ ] 文字切れ、カラーバー、軸、凡例、スクロール位置を目視確認する。
- [ ] 合成データの既知異常7ロットについて判定・診断結果を確認する。
- [ ] 通常ロットが過剰に上位表示されないことを確認する。
- [ ] 全既存テストと新規テストを実行する。
- [ ] 主要処理に対して型チェックを実行する。

### 10.5 Phase 6の受け入れ条件

- [ ] 日常調査に必要なフィルタ、出力、状態保存が利用できる。
- [ ] データ量が増えても主要操作が性能目標内で完了する。
- [ ] 主要な判定・診断フローが自動テストと画面確認で保護されている。

---

## 11. 推奨実装順

- [ ] Step 1：Phase 0で良否、通常性、優先度、比較条件を確定する。
- [ ] Step 2：Phase 1でロット判定集計とテストを完成させる。
- [ ] Step 3：Phase 2で判定散布図、優先度一覧、判定理由を実装する。
- [ ] Step 4：Phase 3で対象対通常の比較、単一フレームマップ、個片詳細を実装する。
- [ ] Step 5：Phase 4で相関、周期性、変化点を必要性の高い順に追加する。
- [ ] Step 6：Phase 5で事例データと類似度を実装する。
- [ ] Step 7：Phase 6で出力、性能、総合テストを仕上げる。

## 12. MVP完了条件

- [ ] 規格適合率と通常異常度の2軸で全ロットを確認できる。
- [ ] 規格不適合、通常逸脱、両方のロットを識別できる。
- [ ] 調査優先度順にロットを選択できる。
- [ ] 選択ロットの主要な異常vision、カテゴリ、検査項目を確認できる。
- [ ] 対象ロットと過去Nロットの通常状態を比較できる。
- [ ] 異常が現れたFrameNo、PositionX、PositionYを特定できる。
- [ ] 測定適合率と個片良品率を区別できる。
- [ ] 24フレーム集約マップと選択フレームマップを区別できる。
- [ ] 判定から診断まで選択状態を維持して移動できる。
- [ ] 既知異常を用いた分析テストと主要UIテストが通る。
